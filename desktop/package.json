{
  "name": "@vibeos/desktop",
  "version": "0.0.0",
  "private": true,
  "description": "vibeOS Electron desktop app — main, renderer, daemon, children. Will be populated by cycle 3 migration.",
  "license": "MIT",
  "scripts": {
    "build": "echo 'TODO: cycle 3 migrates desktop build pipeline' && exit 0",
    "type-check": "echo 'TODO: cycle 3 migrates type-check' && exit 0",
    "test": "echo 'TODO: cycle 3 migrates tests' && exit 0",
    "lint": "echo 'TODO: cycle 3 migrates lint' && exit 0",
    "clean": "rm -rf out dist release node_modules/.cache"
  }
}
